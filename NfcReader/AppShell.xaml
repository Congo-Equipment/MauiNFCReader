<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NfcReader.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:icon="clr-namespace:IconPacks.IconKind;assembly=IconPacks.Material"
    xmlns:local="clr-namespace:NfcReader"
    xmlns:views="clr-namespace:NfcReader.Views"
    Title="CE Clocking">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:ActivityMenuPage}"
        Route="Home" />

    <ShellContent
        Title="Users"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!--<ShellContent
        Title="Meeting Clocking"
        Icon="{Static icon:Material.EventNote}"
        ContentTemplate="{DataTemplate views:MeetingActivity}"
        Route="MeetingActivity" />-->

    <ShellContent
        Title="Clockings"
        ContentTemplate="{DataTemplate views:ClockingsPage}"
        Route="ClockingsPage" />

    <ShellContent
        Title="Settings"
        ContentTemplate="{DataTemplate views:SettingsPage}"
        Icon="{Static icon:Material.Refresh}"
        Route="SettingsPage" />

</Shell>
